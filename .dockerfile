# Files to ignore when building Docker image
# (These won't be copied into the container)

# Git files (not needed in container)
.git
.gitignore

# Documentation (not needed to run the app)
README.md
*.md

# IDE files (editor settings)
.vscode/
.idea/

# Mac system files
.DS_Store

DockerFile
